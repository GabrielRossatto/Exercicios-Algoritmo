algoritmo "semnome"
var
   salario, novo_salario: real
inicio
   escreval("Salario: ")
   leia(salario)
   novo_salario <- (salario*15/100) + salario
   escreval("O novo salario eh :", novo_salario)

fimalgoritmo