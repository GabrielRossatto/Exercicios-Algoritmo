algoritmo "semnome"
var
   num, soma: inteiro
inicio
   repita
      soma <- soma + num
      escreval("Digite um número: ")
      leia(num)
   ate(num = 1111)
   escreval("Soma: ", soma)

fimalgoritmo