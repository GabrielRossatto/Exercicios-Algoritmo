algoritmo "semnome"
var
   a, b, soma: inteiro
procedimento somador(x,y: inteiro)
inicio


   soma <- a + b
   escreval("A soma entre a e b eh: ", soma)

fimprocedimento
inicio
   escreval("Digite um numero: ")
   leia(a)
   escreval("Digite outro numero: ")
   leia(b)
   somador(a,b)
fimalgoritmo